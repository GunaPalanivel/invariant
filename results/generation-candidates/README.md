# Generation candidates

Failed or intermediate model blobs. Not the verified pack.

- `ef1a232-groq/` — recovered Groq unittest from validation commit `ef1a232`. Construction error: `make_session()` returns a tuple. Not claimed as the verified regression.

Live attempt folders are local-only (gitignored). The verified generated file is `generated/test_generated_aut.py`.
